# 🔑 AuthenticationManager: Главный Охранник Spring Security

Это фундаментальный метод в Spring Security, и понимание его работы — **ключ ко всей системе аутентификации**.

Представьте **AuthenticationManager** как главного охранника на входе в ваше приложение. Вы подходите к нему и предъявляете свои "документы". Его задача — проверить их и либо выдать вам пропуск, либо отказать в доступе.

Метод `authenticationManager.authenticate()` — это и есть процесс этой проверки.

---

## ⚙️ Что происходит "под капотом" (По шагам)

Рассмотрим процесс на примере типичного вызова, который вы, вероятно, используете:

```java
// Пример в вашем AuthService или подобном классе
Authentication authentication = authenticationManager.authenticate(
    new UsernamePasswordAuthenticationToken(request.getEmail(), request.getPassword())
);

// Далее: создание JWT и установка в контекст
SecurityContextHolder.getContext().setAuthentication(authentication);
String jwt = jwtService.generateAccessToken(authentication);
````

### Шаг 1: Создание "Запроса на аутентификацию"

Сначала вы создаете объект `UsernamePasswordAuthenticationToken`. На этом этапе это просто **"контейнер"** для данных, предоставленных пользователем (логин и пароль).

> 📝 **Важно:** В этот момент токен **неаутентифицирован**. Это всего лишь запрос: "Эй, проверьте, пожалуйста, вот эти логин и пароль".

### Шаг 2: AuthenticationManager ищет подходящего "Проверяющего"

**AuthenticationManager** — это интерфейс. Его стандартная реализация в Spring — это класс **ProviderManager**.

**ProviderManager** не проверяет данные сам. У него есть список **"проверяющих"** — `AuthenticationProvider`'ов. Каждый провайдер специализируется на определенном типе аутентификации.

Когда ProviderManager получает ваш `UsernamePasswordAuthenticationToken`, он опрашивает всех "специалистов": **"Ты умеешь работать с таким типом запроса?"**

### Шаг 3: DaoAuthenticationProvider берется за работу

Для `UsernamePasswordAuthenticationToken` откликается самый распространенный провайдер — **DaoAuthenticationProvider**. Именно он выполняет основную логику проверки по базе данных (**DAO** - Data Access Object).

#### 3.1. Найти пользователя

* Он использует сервис **UserDetailsService**, а точнее ваш **CustomUserDetailService**, который имплиментирует **UserDetailsService**, который требует Spring Security. Так **DaoAuthenticationProvider** поймет, что куда ему обращаться и сможет получить данные из БД. 
* @Service: Он помечен как бин, поэтому Spring Security может его найти и "подключить" к AuthenticationManager.
* Вызывается `customUserDetailsService.loadUserByUsername(username)`, куда передается email из токена. Это тот самый метод, который вызывает DaoAuthenticationProvider (внутри authenticationManager).

  * Он идет в UserRepository и ищет пользователя в базе данных. Ваш код очень гибкий, так как ищет и по имени, и по почте.
  * Если пользователь не найден, бросается исключение UsernameNotFoundException, как и положено по контракту UserDetailsService.
  * Если пользователь найден, он не возвращает напрямую сущность User. Вместо этого он вызывает CustomUserPrincipal.create(user).
  

* CustomUserPrincipal.create(user): Этот метод, скорее всего, преобразует вашу сущность User в объект UserDetails — стандартный "паспорт" пользователя для Spring Security. UserDetails содержит методы getUsername(), getPassword(), getAuthorities(), которые DaoAuthenticationProvider использует для завершения процесса аутентификации (сравнения пароля и установки прав).

#### 3.2. Проверить пароль

Поскольку пользователь найден, как мы уже сказали, **DaoAuthenticationProvider** получает две вещи:

1.  Пароль в открытом виде от пользователя (из `UsernamePasswordAuthenticationToken`).
2.  **Хэш** пароля из базы данных (из объекта `UserDetails`).

Он использует третий сервис — бин **PasswordEncoder** (например, `BCryptPasswordEncoder`).

* Он хэширует пароль пользователя.
* Сравнивает полученный хэш с хэшем из БД.

### Шаг 4: Результат проверки

| Сценарий | Результат | Действие |
| :--- | :--- | :--- |
| **Неудача** | Пароли не совпали. | DaoAuthenticationProvider бросает исключение **BadCredentialsException**. Метод `authenticate()` завершается с ошибкой. |
| **Успех\!** | Пароли совпали. | DaoAuthenticationProvider создает **НОВЫЙ** объект `UsernamePasswordAuthenticationToken`. |

> ✅ **Самое главное:** У нового токена выставляется флаг `authenticated = true`. Он полностью заполнен объектом `UserDetails` и правами/ролями (**authorities**) пользователя.

-----

## 🎯 Итог: Что вы получаете?

Переменная `authentication`, которую вы получаете после успешного вызова `authenticate()`, — это уже не просто запрос, а **полноценное подтверждение личности пользователя**.

Это **"пропуск"**, который:

* Содержит всю необходимую информацию о пользователе (кто он, какие у него роли).
* Имеет официальную "печать" от системы безопасности (`authenticated = true`).

Именно этот **доверенный** объект вы затем используете для:

1.  Установки в `SecurityContextHolder` для текущей сессии.
2.  Генерации **JWT** (JSON Web Token), который унаследует все данные о пользователе и его правах.

<!-- end list -->
